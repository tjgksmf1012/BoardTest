# 포인트라운지 실행 방법

글을 쓰고 댓글을 달면 포인트가 쌓이고, 그 포인트로 아바타를 꾸미는 커뮤니티 게시판입니다.
아래 세 단계만 하시면 컴퓨터에서 바로 띄워 보실 수 있습니다. 윈도우든 맥이든 똑같습니다.

## 1단계. Node.js 설치 (처음 한 번만)

https://nodejs.org 에 들어가서 LTS 버전을 받아 설치하세요. 20 버전 이상이면 됩니다.

설치가 됐는지 보시려면 터미널(윈도우는 명령프롬프트)에서 이렇게 쳐 보세요.

```
node -v
```

`v20` 이나 `v22` 로 시작하는 게 나오면 된 겁니다.

## 2단계. 실행

프로그램 폴더 안에서 터미널을 열고 아래 두 줄을 차례로 치세요.

```
npm install
npm start
```

첫 줄은 필요한 라이브러리를 받아 오는 것인데 처음 한 번만 하면 되고 5초에서 10초쯤 걸립니다.
둘째 줄이 서버를 켜는 것입니다. `게시판 서버 실행 중: http://localhost:3000` 이 뜨면 성공입니다.

## 3단계. 접속

브라우저 주소창에 이렇게 치시면 됩니다.

```
http://localhost:3000
```

끄실 때는 터미널에서 Ctrl 키와 C 키를 같이 누르세요.

## 미리 만들어 둔 데모 계정
| 구분 | 아이디 | 비밀번호 | 설명 |
|---|---|---|---|
| 운영자 | `admin` | `admin1234` | 공지·숨김·회원 제재 등 운영자 기능 |
| 회원 | `cherry` | `test1234` | 포인트 1,800P |
| 회원 | `mint` | `test1234` | 포인트 6,200P (스페셜 헤어 해금) |
| 회원 | `gold` | `test1234` | 포인트 12,500P (프리미엄 의상 해금) |

처음 실행하면 공지랑 샘플 게시글이 알아서 들어가 있어서 바로 둘러보실 수 있습니다.
새로 회원가입하시면 1,000P 를 받고 시작합니다.

## 무엇을 볼 수 있나요

글쓰기, 댓글, 추천, 스크랩이 되고 말머리랑 베스트댓글도 있습니다. 출석체크로 포인트를 모아서
마이페이지에서 아바타를 해금할 수 있고, 레벨이랑 업적 배지, 포인트 랭킹, 알림도 들어 있습니다.
`admin` 으로 로그인하시면 신고 처리, 회원 관리, 글 숨김 같은 운영자 기능도 보실 수 있습니다.

## 잘 안 될 때

`npm` 을 못 찾겠다고 나오면 Node.js 가 안 깔린 것입니다. 1단계를 다시 해 주세요.

`npm install` 에서 멈추거나 오류가 나면 인터넷이 연결돼 있는지 보시고 다시 해 보세요.
`node -v` 를 쳐서 20 이상인지도 확인해 보시고요.

3000 포트를 이미 쓰고 있다고 나오면 다른 번호로 띄우시면 됩니다.
맥이나 리눅스면 `PORT=4000 npm start`, 윈도우 파워셸이면 `$env:PORT=4000; npm start` 로
띄우신 다음 `http://localhost:4000` 으로 들어가세요.

더 자세한 건 `README.md` 에 있고, 어떻게 설계했는지는 `docs/DESIGN.md` 에 있습니다.
