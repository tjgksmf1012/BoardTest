# 포인트라운지 — 실행 방법

활동으로 포인트를 모아 아바타가 성장하는 커뮤니티 게시판입니다.
아래 3단계면 컴퓨터에서 바로 실행해볼 수 있어요. (Windows · Mac 공통)

---

## 1단계. Node.js 설치 (최초 1회)
- https://nodejs.org 에서 **LTS 버전(20 이상)** 다운로드 후 설치
- 설치 확인: 터미널(명령프롬프트)에서
  ```
  node -v
  ```
  `v20...` 또는 `v22...` 이 나오면 준비 완료

## 2단계. 실행
프로그램 폴더 안에서 터미널을 열고, 아래 두 줄을 차례로 실행하세요.
```
npm install
npm start
```
- `npm install` : 필요한 라이브러리 설치 (처음 한 번만, 약 5~10초)
- `npm start` : 서버 실행 →  `게시판 서버 실행 중: http://localhost:3000` 이 뜨면 성공

## 3단계. 접속
웹 브라우저 주소창에 입력:
```
http://localhost:3000
```

종료하려면 터미널에서 **Ctrl + C** 를 누르세요.

---

## 미리 만들어 둔 데모 계정
| 구분 | 아이디 | 비밀번호 | 설명 |
|---|---|---|---|
| 운영자 | `admin` | `admin1234` | 공지·숨김·회원 제재 등 운영자 기능 |
| 회원 | `cherry` | `test1234` | 포인트 1,800P |
| 회원 | `mint` | `test1234` | 포인트 6,200P (스페셜 헤어 해금) |
| 회원 | `gold` | `test1234` | 포인트 12,500P (프리미엄 의상 해금) |

- 처음 실행하면 공지·샘플 게시글이 자동으로 들어가 있어 바로 둘러볼 수 있어요.
- 새로 회원가입하면 1,000P를 받고 시작합니다.

## 무엇을 볼 수 있나요
- 글쓰기·댓글·추천·스크랩, 말머리(카테고리), 베스트댓글
- 출석체크로 포인트 적립, 포인트로 **아바타 등급 해금**(마이페이지)
- 레벨·업적 배지, 포인트 랭킹, 알림
- 운영자(admin)로 로그인하면 신고 관리·회원 관리·글 숨김 처리

---

## 잘 안 될 때
- **`npm` 을 찾을 수 없다** → Node.js가 설치 안 된 것. 1단계 다시.
- **`npm install` 에서 멈춤/오류** → 인터넷 연결 확인 후 다시 시도. Node 버전이 20 이상인지 `node -v` 로 확인.
- **3000 포트가 이미 사용 중** → 다른 포트로 실행:
  - Mac/Linux: `PORT=4000 npm start`
  - Windows(PowerShell): `$env:PORT=4000; npm start`
  그 후 `http://localhost:4000` 접속.

> 더 자세한 내용은 `README.md`, 설계 문서는 `docs/DESIGN.md` 를 참고하세요.
