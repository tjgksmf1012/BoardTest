# 배포 방법

다른 사람이 주소만 알면 들어와서 써 볼 수 있게 인터넷에 올리는 방법입니다.

지금 올려 둔 주소는 https://pointlounge.onrender.com 입니다.
누구나 바로 들어가서 써 보실 수 있습니다. 데모 계정은 운영자가 `admin / admin1234`,
일반 회원이 `gold / test1234` 입니다. mint, cherry, street 도 비밀번호는 `test1234` 입니다.

이 프로그램은 특별할 게 없는 보통 웹 서버라, 컨테이너를 올릴 수 있는 호스팅이면 어디든 됩니다.
아래는 공짜로 제일 쉽게 할 수 있는 Render.com 기준으로 적었습니다.
저장소에 `render.yaml` 파일이 이미 들어 있어서 설정할 게 거의 없습니다.

## Render 에 올리기 (5분쯤 걸립니다)

1. https://render.com 에 가입하거나 로그인합니다. GitHub 계정으로 로그인하시면 편합니다.
2. 위쪽 New + 를 누르고 Blueprint 를 고릅니다.
3. 이 GitHub 저장소(`BoardTest`)를 고르고 Apply 를 누릅니다.
   `render.yaml` 을 알아서 읽어서 `pointlounge` 라는 웹 서비스를 만들어 줍니다.
   `SESSION_SECRET` 도 Render 가 알아서 만들어 넣습니다.
4. 빌드가 끝나면 몇 분 뒤에 `https://pointlounge-xxxx.onrender.com` 같은 주소가 나옵니다.
5. 그 주소를 교수님이나 테스트해 주실 분께 알려 드리면 됩니다.

몇 가지 알아 두실 게 있습니다. 공짜 플랜은 15분 동안 아무도 안 들어오면 서버가 잠듭니다.
그래서 다음에 처음 들어가는 사람은 30초쯤 기다려야 하는데 고장 난 게 아니라 원래 그렇습니다.

그리고 공짜 플랜은 저장 공간이 임시라서 서버가 다시 뜨면 그동안 쓴 글이 다 없어지고
처음의 데모 데이터로 돌아갑니다. 테스트용으로는 이걸로 충분합니다.
글이 계속 남아 있어야 하면 유료 디스크나 외부 DB 로 바꾸면 되는데,
보여 드리고 제출하는 용도면 굳이 안 하셔도 됩니다.

## 다른 곳에 올려도 됩니다

Railway 나 Fly.io 처럼 컨테이너를 올릴 수 있는 곳이면 어디서든 돌아갑니다.
`Dockerfile` 이 같이 들어 있습니다.

인터넷에 안 올리고 내 컴퓨터에서만 돌려 보실 거면 배포 안 하셔도 됩니다.
`실행방법.md` 대로 `npm install` 하고 `npm start` 만 하시면 됩니다.

## 데모 계정 (배포 후 바로 테스트)
| 구분 | 아이디 | 비밀번호 |
|---|---|---|
| 운영자 | `admin` | `admin1234` |
| 일반 회원 | `gold` | `test1234` |
| 일반 회원 | `mint` / `cherry` / `street` | `test1234` |
